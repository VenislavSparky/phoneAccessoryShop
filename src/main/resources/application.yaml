spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/phoneAccessoryShopDB?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: 1234
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:

logging:
  level:
    org.hibernate.SQL:
    org.hibernate.orm.jdbc.bind:
    org.springframework.security:

mail:
  phoneAccessoryMail: "phoneAccessoryBulgaria@example.com"
  host: localhost
  port: 1025
  username: ${MAIL_USER:testuser}
  password: ${MAIL_PASSWORD:testpass}
